var React = require('react');

var HelloWorldButton = React.createClass({
    render: function () {
        return (
            <button>Clique aqui</button>
        );
    }
});

module.exports = HelloWorldButton;